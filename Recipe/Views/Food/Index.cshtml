@{
  Layout = "_Layout";
}
@using Recipe.Models;

@model List<Recipe.Models.Food>;

<h1>Recipes</h1>

@if (@Model.Count == 0)
{
  <h3>No recipes have been added yet!</h3>
} 
@* @if (AppUser.Identity.GetUserId() == Model.UserId )
{ *@
@foreach (Food food in Model)
@* { *@
  <li>@Html.ActionLink($"{food.FoodName}: {food.Ingredients}", "Details", new { id = food.FoodId })</li>
}
}
<p>@Html.ActionLink("Add new recipe", "Create")</p>
<p>@Html.ActionLink("Home", "Index", "Home")</p>